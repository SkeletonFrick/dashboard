# backend/services/notification_service.py

async def send_notification(canal: str, message: str) -> bool:
    """
    Stub — notifications externes non implémentées.
    Conservé pour compatibilité future (Phase 4A complète).
    """
    return False